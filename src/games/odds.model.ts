export class OddsModel {
    constructor(
        private first: string,
        private second: string,
        private image: string,
        private type: string,
        private url: string,
    ) {}
}
